import "./chunk-PGWBAY6J.js";

// node_modules/vscode/override/iconv.js
var textDecoder = new TextEncoder();
var textEncoder = new TextDecoder();
var encoder = {
  write(str) {
    return textDecoder.encode(str);
  },
  end() {
    return void 0;
  }
};
var decoder = {
  write(str) {
    return textEncoder.decode(str);
  },
  end() {
    return void 0;
  }
};
function checkEncoding(encoding) {
  if (!encodingExists(encoding)) {
    throw new Error(`Encoding not found: ${encoding}`);
  }
}
var getEncoder = (encoding) => {
  checkEncoding(encoding);
  return encoder;
};
var getDecoder = (encoding) => {
  checkEncoding(encoding);
  return decoder;
};
var encodingExists = (encoding) => {
  return encoding === "utf8";
};
var decode = (buffer, encoding) => {
  checkEncoding(encoding);
  return decoder.write(buffer);
};
var encode = (buffer, encoding) => {
  checkEncoding(encoding);
  return encoder.write(buffer);
};
export {
  decode,
  encode,
  encodingExists,
  getDecoder,
  getEncoder
};
//# sourceMappingURL=iconv-RPG6PRHC.js.map
